import React from 'react';
import classes from './SettingsMenuLoader.module.css';

const SettingsMenuLoader = () => {
    return (
        <div className={classes.loader}>
            Loading...
        </div>
    );
};

export default SettingsMenuLoader;